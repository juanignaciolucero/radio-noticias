import radios.backend.AdMetadata

model {
    AdMetadata adMetadata
}

json {
    url_redirect adMetadata.urlRedirect
    if (adMetadata.image) {
        image tmpl.'/multimedia/multimedia'(multimedia: adMetadata.image)
    } else {
        image [:]
    }

}
